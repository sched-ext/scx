# List of scx_schedulers: scx_beerland scx_bpfland scx_cake scx_cognis scx_chaos scx_cosmos scx_flash scx_lavd scx_layered scx_mitosis scx_p2dq scx_pandemonium scx_rlfifo scx_rustland scx_rusty scx_tickless scx_wd40
SCX_SCHEDULER=scx_cosmos

# Set custom flags for each scheduler, below is an example of how to use
SCX_FLAGS='-s 700 -S'
