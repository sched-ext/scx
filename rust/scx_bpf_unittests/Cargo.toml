[package]
name = "scx_bpf_unittests"
version = "1.1.1"
edition = "2021"

publish = false

[dependencies]
libbpf-sys = "=1.7.0"  # need the includes and can't use libbpf_rs instead

[build-dependencies]
cc = "1"
indoc = "2"
object = "0.39"
